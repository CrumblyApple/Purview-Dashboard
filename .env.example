# Frontend (optional — defaults work for local dev via Vite proxy)
# VITE_API_BASE_URL=/api

# Vite dev server proxy target (used in docker-compose.dev.yml)
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
